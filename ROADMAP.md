# MEETAMASK — ROADMAP (product + process)

> Источник истины по итерациям. Методология (AGENTS.md): одна итерация = одна проверяемая цель;
> user stories до кода; TDD где применимо; codex-ревью диффа; атомарный коммит; апдейт PROGRESS.md.
> Цели founder: (1) аппкой кайфово пользоваться, (2) юзер может лепить свои маски, (3) лучше процесс.

## Где мы (факт, 2026-06-10)
- ✅ Ядро работает: маска → движок → shm → виртуалкамера, без фриза.
- ✅ Движок раздаётся: notarized, GitHub Release `v0.2`, URL вписан в `EngineInstaller`.
- ✅ 3 теста зелёные (seqlock tear-free, mask-policy, IP-гард) — но гоняются руками.
- ❌ Продукт вокруг ядра — отладочный стенд: 2 вкладки, лог-дамп, RU/EN вперемешку, плоский список 49 масок.
- ❌ «Свои маски» закрыты by design (правильно: движок `no_sandbox` + `allow-file-access` + авто-камера, без сетевого фильтра).
- ❌ Нет CI, релиз руками, рассинхрон notary-профиля.

---

## МАСТЕР-ЧЕК-ЛИСТ (порядок = приоритет impact/effort)

### Quick wins (фундамент + первый «вау»)
- [x] **I1 — CI на push** (GitHub Actions гоняет 3 теста + сборку app без подписи). ✅ зелёный.
- [x] **I2 — Гигиена процесса** (фикс рассинхрона notary-профиля; `make test-all`). ✅
- [ ] **I3 — Теги** (`tags: [String]` в модели + `meetamask.json` на маску + проставить теги всем 49). Дизайн: `DESIGN.md`.
- [ ] **I4 — Экран «Сцена», структура** (убрать TabView; один экран: левая ON AIR-панель + сетка 4-в-ряд + тег-фильтр + поиск + избранное; честная статус-машина live/switching/paused с подтверждением первого кадра; EN-строки; окно ≥1080).
- [ ] **I5 — Glass + бренд-полировка** (бандл шрифтов Schibsted/Hanken, glassEffect-материалы, микро-анимации, пайплайн статичных превью для всех 49).
- [ ] **I6 — Системные состояния в языке «Сцены»** (approval-лист расширения, прогресс загрузки движка, permission/error — стеклянные листы поверх затемнённой сцены).

### Big bets (стержень продукта)
- [ ] **I7 — A0 ГЕЙТ БЕЗОПАСНОСТИ** (`no_sandbox=false` + `engine/request_handler.*` с CSP + сетевым allow-list; файлы — только в папке маски). *Обязателен ПЕРЕД любым пользовательским контентом.*
- [ ] **I8 — A1 Preset Maker** (выбрал эффект → подвигал ползунки → сохранил пресет; `approvedMasks` валидирует, а не отвергает всё). *Та самая «одна вещь».*
- [ ] **I9 — Онбординг-оверхол** (один флоу вместо 2 вкладок, hero-маска, меню-бар тоггл + хоткей).
- [ ] **I10 — Авторелиз** (headless Developer ID подпись через ASC API key; tag-триггерный workflow).
- [ ] **I11 — Импорт кастомного HTML-маски** (file/zip → манифест + sha256 + подпись; крутится только в песочнице I7).
- [ ] **I12 — Виральность** (запись/шеринг клипа; жесты-триггеры; ползунок интенсивности).

> Детализируем итерацию, когда до неё доходим (Ralph-loop). Ниже — детально I1–I4 (ближайшие).

---

## I1 — CI на push  ← ТЕКУЩАЯ
**User story.** Как разработчик, я хочу, чтобы каждый push прогонял тесты и сборку автоматически, чтобы регрессия (особенно в security-критичной mask-policy) ловилась до релиза, а не глазами.

**Критерии приёмки.**
- `.github/workflows/ci.yml` есть; триггерится на push и pull_request.
- Job `tests` (macOS): прогоняет 3 теста — IP-гард, mask-policy (`swiftc`), seqlock (`clang++`) — все зелёные.
- Job `build` (macOS): `xcodegen generate` + `xcodebuild build` без подписи (`CODE_SIGNING_ALLOWED=NO`) — компиляция app проходит.
- Зелёный значок на коммите в `metawhisp/meetmask`.

**Верификация.** Запушить → `gh run watch` → оба job green. Корнер: если build-job флапает (подпись/xcodegen) — чинить до зелёного, не оставлять red.

**Corner cases.** runner без xcodegen (ставим через brew); sysext-таргет с `CODE_SIGNING_ALLOWED=NO`; путь `/tmp` для бинарей тестов.

**Статус:** ✅ закрыта (CI зелёный: tests 13s, build 48s).

## I2 — Гигиена процесса  ← ТЕКУЩАЯ
**User story.** Как мейнтейнер, я хочу один источник правды для тестов и согласованные имена профилей, чтобы скрипты не падали из-за дрейфа.
**Приёмка.** `notarize.sh`/`release-notarize.sh`/`setup-notary.sh` используют одно имя профиля (`meetamask`); добавлен `make test-all` (или `dist/test-all.sh`), который CI и вызывает. **Статус:** ✅ закрыта.

## I3 — Слой метаданных масок  ← СЛЕДУЮЩАЯ (продуктовая → покажу user stories до кода)
**User story.** Как юзер, я хочу видеть у масок категорию/описание/теги, чтобы понимать, что это и когда юзать.
**Приёмка.** `Mask` += `tags: [String]` (свободные, авторские — категории отменены founder'ом). Источник: `Masks/<id>/meetamask.json` если есть, иначе пустые (TDD на парсинг + дефолты). Все 49 встроенных получают теги. Существующее не ломается. **Статус:** ⬜

## I4 — Экран «Сцена» (структура)
**User story.** Как юзер, я хочу один тёмный экран: слева живое превью с честным статусом эфира, справа сетка масок 4-в-ряд с тегами/поиском/избранным — чтобы выбор и контроль были в одном месте.
**Приёмка.** По `DESIGN.md`: TabView убран; статус-машина live/switching/paused (ON AIR только после первого реального кадра новой маски; на паузе кнопка `Go live`); подсказка «Choose MEETAMASK Camera in Meet» при неготовом sink; превью карточек статичные; EN-строки; окно ≥1080. **Статус:** ⬜

---

## Лог итераций
- **I1 — CI (2026-06-10).** Добавлен `.github/workflows/ci.yml`: job `tests` (IP-гард + mask-policy `swiftc` + seqlock `clang++`) и job `build` (xcodegen + `xcodebuild build` без подписи). Оба зелёные на `metawhisp/meetmask` (tests 13s, build 48s). `checkout@v5` (Node 24). Коммиты `6408ea2`, далее бамп checkout. Узнал: workflow-файлы пушатся только keyring-токеном `gh` (у именованного нет scope `workflow`).
  - **codex-ревью итерации поймал P2:** seqlock-тест выходил `1` и при реальном баге, и при «inconclusive» (гонка не воспроизвелась на раннере) → CI мог краснеть без регрессии. Фикс: тест теперь возвращает `2` на inconclusive, `1` на реальный разрыв; CI ретраит на `2`, падает на `1`. Детерминированный зелёный.
- **I2 — Гигиена (2026-06-10).** `tests/run-all.sh` (+ `make test-all`) — единый раннер 3 тестов с seqlock-ретраем; CI теперь зовёт его (DRY: локалка = CI). Фикс рассинхрона notary-профиля: `notarize.sh` дефолт `meetamask-notary` → `meetamask` (как реально настроен профиль), README выровнен.
